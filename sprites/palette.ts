/**
 * マスコットのパレット。
 *
 * 当初は「16色以内」としていたが、これは**ドット絵を数値配列で配る前提の制約**だった。
 * 実際には絵を実行時に計算で描いているので、色数は配信量に一切影響しない。
 * キャラを増やすと基板の緑や端子の金など必要な色が出てくるため、上限は外した。
 * ただし**役割ごとに1色**（同じ役割で似た色を増やさない）という方針は保つ。
 *
 * このサイトの背景は #07090e の暗色なので、**輪郭の暗色に頼って形を見せることができない**
 * （背景に溶ける）。シルエットは明るい色（ハブ＝顔）側で立たせ、暗い輪郭は
 * カード背景(#0e1118)の上に乗ったときの境目としてだけ効かせる。
 *
 * 添字がそのままフレーム配列の値になる。0 は必ず透過。
 */
export const MASCOT_PALETTE = [
  "transparent", //  0 透過
  "#0c1018",     //  1 輪郭
  "#6d7c96",     //  2 ファン枠(影)
  "#94a3bd",     //  3 ファン枠(基本)
  "#c2cee0",     //  4 ファン枠(ハイライト)
  "#202836",     //  5 羽根の隙間＝奥の影
  "#4e5c76",     //  6 羽根(基本)
  "#eef3fb",     //  7 ハブ＝顔(基本)
  "#c0cbdc",     //  8 ハブ(影)
  "#ffffff",     //  9 ハブ(ハイライト)
  "#5dd8bb",     // 10 アクセント（サイトの --accent と同値。四隅のLED）
  "#1b2532",     // 11 目・口のインク
  "#f2909f",     // 12 ほお
  "#e7c84b",     // 13 注意（相性が微妙）— サイトの --warn と同値
  "#f05070",     // 14 不可（相性が合わない）— サイトの --danger と同値
  "#2e4a3c",     // 15 基板の緑（濃）
  "#3d6350",     // 16 基板の緑（淡）
  "#d8b25c",     // 17 金メッキの端子
] as const;

/** パレット添字の別名。描画コードで数字を直書きしないための定数。 */
export const C = {
  none: 0,
  outline: 1,
  frameDark: 2,
  frame: 3,
  frameLight: 4,
  bladeGap: 5,
  blade: 6,
  bladeEdge: 2,
  hub: 7,
  hubDark: 8,
  hubLight: 9,
  accent: 10,
  ink: 11,
  cheek: 12,
  warn: 13,
  danger: 14,
  pcb: 15,
  pcbLight: 16,
  gold: 17,
} as const;

/** ASCII での確認用。パレット添字 → 1文字。scripts/mascot-preview.mjs と対応。 */
export const PALETTE_CHARS = ".#23456789A*o" as const;
